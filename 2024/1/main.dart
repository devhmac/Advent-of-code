// locations listed by ID
// compare two lists
// - pair up numbers and measure delta
// - each num unique location

// compare sorted lists (smallest v smallest)
//sum delta
import 'dart:io';

String get input {
  File input = new File('./exampleInput.txt');
  return input.readAsStringSync();
}

main() async {
  print(input);
}
